subset_of_shots: 
local: true

cnn_settings:
  D: 16

dataloader_setting:
  batch_size: 512
  num_workers: 8 #16
  # shuffle: True

training_args:
  lr: 0.0001
  # max_epochs: 500
  max_steps: 10000000
  patience: 10

paths:
  data_output_directory: /output_without_sparsity/cnn_model_iterable_lr_4_batch_512