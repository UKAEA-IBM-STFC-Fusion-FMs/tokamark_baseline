subset_of_shots: 10
local: true

lstm_settings:
  D: 16

dataloader_setting:
  batch_size: 8
  num_workers: 0
  # batch_size: 64 
  # num_workers: 64
  # shuffle: True 

training_args:
  # lr: 0.00001
  lr: 0.001 # was working with 0.001
  max_steps: 200000 #500
  patience: 5

paths:
  # data_output_directory: /output/cnn_model_test
  data_output_directory: /output_NEW_vfinal/cnn_model_iterable_lr_4_batch_512